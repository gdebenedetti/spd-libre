{{document-title title="SPD"}}

<header>
	<nav>
		<ul>
			{{#if session.isAuthenticated}}
			  <li>{{session.user.nombre}} {{session.user.apellido}} <a {{ action 'invalidateSession' }}>Logout</a></li>
			  <li>{{#link-to 'proyectos.index'}} Mis Proyectos {{/link-to}}</li>
			{{else}}
			  <li>{{#link-to 'login'}}Login{{/link-to}}</li>
			{{/if}}
		</ul> <!-- main-nav -->
	</nav><!-- main-nav-wrap -->
</header>

{{outlet 'main'}}
