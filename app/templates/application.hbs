{{document-title title="SPD"}}

{{#if session.isAuthenticated}}
<nav {{bind-attr class="isShowMenu:show"}}>
	{{#link-to 'index' class="app-logo"}}
		<img src="assets/images/logo-hcdn.png">
	{{/link-to}}

	{{#if session.isAuthenticated}}
	  <ul class="main-nav">
		<li>
			{{#link-to 'proyectos.index'}}
				{{fa-icon 'folder-o'}}
				<span>Proyectos</span>
			{{/link-to}}
		</li>  	
		<li >
			{{#link-to 'comisiones.index'}}
				{{fa-icon 'comments'}}
		  		<span>Comisiones</span>
	  		{{/link-to}}
		</li>  	
		<li>
	  		{{#link-to 'ordenes-del-dia.index'}}
				{{fa-icon 'list-ul'}}
		  		<span>Ordenes del día</span>
	  		{{/link-to}}
		</li>  	
		<li class="disabled">
	  		<a>
				{{fa-icon 'file-text-o'}}
	  			<span>Planes de labor</span>
	  		</a>
		</li>  	
	  </ul>
	{{/if}}
</nav>

<header>
	<div class="action-bar">
		<a class="smart-search" {{action 'toggleMenu'}}>
			{{fa-icon 'bars'}} 
		</a>
	</div>
	<div class="user-login">
		{{#if session.isAuthenticated}}
		  	{{session.user.nombre}} ({{session.user.diputado}}) <a {{ action 'invalidateSession' }}>Salir</a>
		{{else}}
	  		{{#link-to 'login'}}Ingresar{{/link-to}}
		{{/if}}
	</div>
</header>


{{liquid-outlet class="main-content"}}

{{else}}

{{liquid-outlet class="login-container"}}

{{/if}}

{{outlet 'error'}}

